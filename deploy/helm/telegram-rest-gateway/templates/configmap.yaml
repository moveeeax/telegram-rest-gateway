apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tgw.fullname" . }}-config
  labels:
    {{- include "tgw.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
