# Пин версии TDLib — ЕДИНЫЙ источник истины (читается CMake и Docker).
# ОБЯЗАТЕЛЬНО: перед первой реальной сборкой заменить на ПОЛНЫЙ git-SHA конкретного
# коммита tdlib/td (не ветку!) — иначе сборки невоспроизводимы (см. blocker #2).
# Значение ниже — плейсхолдер-ветка; воспроизводимость НЕ гарантируется, пока не SHA.
TDLIB_REF=master
# Ожидаемая версия TL-схемы для рантайм-проверки getOption("version"):
TDLIB_EXPECTED_VERSION=1.8.0
